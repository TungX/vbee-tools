<h1 id="type-action" data-type-name="<?php echo $this->type_name; ?>">
    Danh sách voice
    <button class="btn btn-success pull-right" id="synchronize">Đồng bộ</button>
</h1>
<a href="#add" class="pull-right" id="add_object">+ Thêm voice</a>
<div class="row"></div>

<?php echo $this->partial("layout/search.phtml", array('fields' => array('name' => 'Tên'))); ?>
<table class="table table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Đồng bộ</th>
            <th>Tên</th>
            <th></th>
        </tr>
        <?php echo $this->partial("voice/info.phtml", array('order' => 0, 'voice' => array(), 'class' => 'info-pattern')); ?>
    </thead>
    <tbody>
        <?php
        $order = 1;
        foreach ($this->voices as $voice) {
            echo $this->partial("voice/info.phtml", array('order' => $order, 'voice' => $voice));
            $order++;
        }
        ?>
    </tbody>
</table>
<?php
echo $this->partial("layout/process_syn.phtml", array());
echo $this->partial("layout/modal_info.phtml", array('title' => 'Thêm voice ','action'=>'/voice/',
    'fields' => array('name'=>array('type'=>'fileUpload', 'option'=>'Voice')),
    'controller_name' => 'voice'));
echo $this->partial("layout/modal_remove.phtml", array('title' => 'Xóa voice '));
echo $this->partial("dictionary/modal_control.phtml", array('title' => 'Đồng bộ voice',));
